import { Text } from 'react-native';

import { ScreenShell } from '../components/ScreenShell';

export function ActivityScreen() {
  return (
    <ScreenShell title="Activity" subtitle="Phase 0 placeholder for delivery history and filters.">
      <Text style={{ color: '#9AA7CC' }}>Activity feed and filters arrive in Phase 6.</Text>
    </ScreenShell>
  );
}
