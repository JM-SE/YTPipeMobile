import { Text } from 'react-native';

import { ScreenShell } from '../components/ScreenShell';

export function ChannelsScreen() {
  return (
    <ScreenShell title="Channels" subtitle="Phase 0 placeholder for channel catalog and monitoring controls.">
      <Text style={{ color: '#9AA7CC' }}>Monitored/Unmonitored tabs and toggles arrive in Phase 4.</Text>
    </ScreenShell>
  );
}
